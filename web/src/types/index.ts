/**
 * Tipe data bersama lintas modul PPG.
 *
 * Tipe domain (Jamaah, Presensi, Penilaian, Hafalan) akan diisi pada fase
 * berikutnya bersamaan dengan skema database (kontrak P0.2). File ini
 * sengaja dibiarkan sebagai titik kumpul agar impor `@/types` stabil.
 */

export {}
